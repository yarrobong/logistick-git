<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Список Заказов</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Темная тема как в Telegram */
        body { 
            background-color: #18222D; 
            color: #ffffff; 
        }

        .order-card {
            background-color: #213040; /* Тёмно-серый как в ТГ */
            border: none; /* Убрана граница */
            border-radius: 0.5rem;
            overflow: hidden;
            height: 100%;
            transition: transform 0.2s, box-shadow 0.2s;
            color: white; /* Белый текст */
        }
        .order-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.5); /* Более тёмная тень */
        }
        .status-badge {
            position: absolute;
            top: 10px;
            right: 10px;
            font-size: 0.8rem;
            /* Убираем bg-классы Bootstrap и используем цвета вручную */
            background-color: #4a4a4a; /* Серый для статуса */
            color: white;
        }
        .order-id {
            font-size: 0.85rem;
            color: #adb5bd; /* Светло-серый */
        }
        .order-details {
            font-size: 0.8rem;
            color: #adb5bd; /* Светло-серый */
        }

        /* Кнопки */
        .btn-primary {
            background-color: #0088cc; /* Сине-зелёный как в ТГ */
            border-color: #0088cc;
        }
        .btn-primary:hover {
            background-color: #0077b3; /* Более тёмный */
            border-color: #0077b3;
        }

        .btn-outline-light {
            border-color: #3a3a3a; /* Тёмная граница */
            color: #d0d0d0; /* Светло-серый текст */
        }
        .btn-outline-light:hover {
            background-color: #3a3a3a; /* Тёмный фон при наведении */
            color: #ffffff; /* Белый текст */
        }

        /* Алерты */
        .alert {
            border: none;
            border-radius: 0.375rem;
        }
        .alert-danger {
            background-color: #2d1e28; /* Тёмно-красный */
            color: #ff6b6b; /* Светло-красный */
        }
        .alert-success {
            background-color: #1e2d21; /* Тёмно-зелёный */
            color: #4caf8b; /* Светло-зелёный */
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Список Заказов</h1>
            <a href="/orders/new" class="btn btn-primary">Создать Заказ</a>
        </div>

        <% if (typeof orders !== 'undefined' && orders.length > 0) { %>
            <div class="row g-3">
                <% orders.forEach(order => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card order-card position-relative h-100">
                            <!-- Тег статуса -->
                            <% const statusConfig = STATUS_CONFIG.ORDER_STATUSES.find(s => s.value === order.status); %>
                            <span class="badge <%= statusConfig ? 'bg-' + statusConfig.color : 'bg-secondary' %> status-badge">
                                <%= order.status %>
                            </span>

                            <div class="card-body d-flex flex-column">
                                <!-- Имя клиента -->
                                <h5 class="card-title mb-1"><%= order.client_name || 'N/A' %></h5>
                                
                                <!-- Город -->
                                <p class="card-text mb-1"><%= order.destination_city || 'N/A' %></p>
                                
                               <!-- Посредник и трек-номер -->
<p class="order-id mb-2">
    <% if (order.intermediary_moscow_destination && order.intermediary_moscow_destination.trim() !== '' && order.tracking_number_moscow_destination && order.tracking_number_moscow_destination.trim() !== '') { %>
        <%= order.intermediary_moscow_destination.trim() %> - <%= order.tracking_number_moscow_destination.trim() %>
    <% } else if (order.intermediary_china_moscow && order.intermediary_china_moscow.trim() !== '' && order.tracking_number_china_moscow && order.tracking_number_china_moscow.trim() !== '') { %>
        <%= order.intermediary_china_moscow.trim() %> - <%= order.tracking_number_china_moscow.trim() %>
    <% } else { %>
        <% if (order.intermediary_moscow_destination && order.intermediary_moscow_destination.trim() !== '') { %>
            <%= order.intermediary_moscow_destination.trim() %> - № трека: не указан
        <% } else if (order.intermediary_china_moscow && order.intermediary_china_moscow.trim() !== '') { %>
            <%= order.intermediary_china_moscow.trim() %> - № трека: не указан
        <% } else { %>
            Посредник: не указан - № трека: не указан
        <% } %>
    <% } %>
</p>

                                <!-- Дополнительная информация -->
                                <div class="order-details mt-auto">
                                    <p class="mb-1">Дата: <%= order.order_date ? new Date(order.order_date).toLocaleDateString('ru-RU') : 'N/A' %></p>
                                    <p class="mb-0">Сумма: <%= (parseFloat(order.total_with_shipping) || 0).toFixed(2) %></p>
                                </div>

                                <!-- Кнопка просмотра -->
                                <div class="mt-3">
                                    <a href="/orders/<%= order.id %>" class="btn btn-outline-light btn-sm">Подробнее</a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p class="text-center">Заказов пока нет.</p>
        <% } %>

        <!-- Flash сообщения -->
        <% if (messages.error.length > 0) { %>
            <% messages.error.forEach(msg => { %>
                <div class="alert alert-danger"><%= msg %></div>
            <% }); %>
        <% } %>
        <% if (messages.success.length > 0) { %>
            <% messages.success.forEach(msg => { %>
                <div class="alert alert-success"><%= msg %></div>
            <% }); %>
        <% } %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>