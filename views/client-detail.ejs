<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Клиент: <%= client.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-dark text-light">
    <%- include('partials/navbar') %> 
    <div class="container mt-4">
        <h1>Клиент: <%= client.name %></h1>
        <div class="row">
            <div class="col-md-6">
                <p><strong>Телефон:</strong> <%= client.phone || 'N/A' %></p>
                <p><strong>Адрес:</strong> <%= client.address || 'N/A' %></p>
            </div>
        </div>

        <hr class="my-4">

        <h2>Заказы клиента</h2>

        <% if (client.orders && client.orders.length > 0) { %>
            <div class="table-responsive">
                <table class="table table-dark table-striped">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Дата</th>
                            <th>Город</th>
                            <th>Статус</th>
                            <th>Сумма (с доставкой)</th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% client.orders.forEach(order => { %>
                            <tr>
                                <td><a href="/orders/<%= order.id %>" class="text-decoration-none"><%= order.id %></a></td>
                                <td><%= order.order_date ? new Date(order.order_date).toLocaleDateString('ru-RU') : 'N/A' %></td>
                                <td><%= order.destination_city || 'N/A' %></td>
                                <td><%= order.status %></td>
                                <td><%= (parseFloat(order.total_with_shipping) || 0).toFixed(2) %></td>
                                <td>
                                    <a href="/orders/<%= order.id %>" class="btn btn-sm btn-outline-light">Детали</a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        <% } else { %>
            <p class="text-center">У этого клиента пока нет заказов.</p>
        <% } %>

        <a href="/clients" class="btn btn-secondary">Назад к списку клиентов</a>

        <!-- Flash сообщения -->
        <% if (messages.error.length > 0) { %>
            <% messages.error.forEach(msg => { %>
                <div class="alert alert-danger"><%= msg %></div>
            <% }); %>
        <% } %>
        <% if (messages.success.length > 0) { %>
            <% messages.success.forEach(msg => { %>
                <div class="alert alert-success"><%= msg %></div>
            <% }); %>
        <% } %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>