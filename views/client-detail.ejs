<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Клиент: <%= client.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Темная тема как в Telegram */
        body { 
            background-color: #18222D; 
            color: #ffffff; 
        }

        /* Стили для карточки клиента */
        .client-card {
            background-color: #213040; 
            border-radius: .5rem;
            padding: 1rem; 
            margin-bottom: 1rem;
        }

        /* Стили для карточек заказов */
        .order-card {
            background-color: #213040; 
            border-radius: .5rem;
            transition: .2s;
            color: white;
            padding: 1rem; 
        }
        .order-card:hover { 
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5); 
            border-color: #3a3a3a; 
        }

        /* Кнопки */
        .btn-outline-light {
            border-color: #3a3a3a; 
            color: #d0d0d0; 
            background-color: transparent;
        }
        .btn-outline-light:hover {
            background-color: #3a3a3a; 
            color: #ffffff; 
        }

        .btn-success {
            background-color: #0088cc; 
            border-color: #0088cc;
        }
        .btn-success:hover {
            background-color: #0077b3; 
            border-color: #0077b3;
        }

        .btn-outline-danger {
            color: #ff3b30; 
            border-color: #ff3b30;
        }
        .btn-outline-danger:hover {
            background-color: #ff3b30; 
            color: #ffffff; 
        }

        /* Вводы */
        .form-control {
            background-color: #213040; 
            border: none; 
            color: #ffffff; 
        }
        .form-control:focus {
            background-color: #213040;
            color: #ffffff;
            box-shadow: 0 0 0 0.2rem rgba(133, 193, 255, 0.25); 
        }

        /* Заголовки */
        h1, h2, h3, h4, h5 {
            color: #ffffff; 
        }

        /* --- НОВЫЕ СТИЛИ ДЛЯ ЗАКАЗОВ --- */
        .order-details {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            align-items: center;
            margin-top: 0.5rem; 
        }

        .order-details div {
            display: flex;
            align-items: center;
            white-space: nowrap;
        }

        /* На мобильных устройствах */
        @media (max-width: 768px) {
            .order-details {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.25rem;
            }

            .order-details div {
                width: 100%; 
                justify-content: flex-start; 
                gap: 0.25rem; 
            }

            .order-details div::before {
                content: attr(data-label) ": "; 
                white-space: nowrap;
            }

            .order-actions {
                opacity: 1; 
                margin-top: 0.5rem; 
                align-self: center; 
            }
        }

        /* Для ширины экрана меньше 1000px */
        @media (max-width: 1000px) {
            .order-card .d-flex.justify-content-between {
                flex-direction: column; 
                gap: 0.5rem;
            }

            .order-card .flex-grow-1 div {
                display: flex;
                flex-direction: column; 
                gap: 0.25rem;
                width: 100%;
            }

            .order-card .flex-grow-1 div .order-info,
            .order-card .flex-grow-1 div .order-stats {
                width: 100%; 
            }

            .order-actions {
                margin-top: 0.5rem;
                width: 100%;
                justify-content: flex-start;
            }
        }

        /* Стили для редактируемых полей */
        .editable-field {
            cursor: pointer;
            border-bottom: 1px dashed transparent;
            display: inline-block;
            min-width: 80px;
            min-height: 1.5em;
            padding: 0.125rem 0.25rem;
            border-radius: 0.25rem;
        }
        .editable-field:hover {
            border-bottom: 1px dashed #85c1ff;
            background-color: rgba(133, 193, 255, 0.1);
        }
    </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #213040;">
    <div class="container-fluid px-3 px-md-4 px-lg-5">
        <div class="d-flex flex-column flex-lg-row justify-content-between w-100">
            <div class="d-flex align-items-center mb-2 mb-lg-0">
                <a class="navbar-brand fs-4 fw-bold" href="/">Логистика BIZON</a>
                <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto me-lg-4">
                    <li class="nav-item">
                        <a class="nav-link fs-6" href="/orders">Заказы</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link fs-6" href="/clients">Клиенты</a>
                    </li>
                </ul>

                <!-- Блок с пользователем и выходом внутри меню -->
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <span class="navbar-text me-3 text-light">
                            Привет, <%= typeof session !== 'undefined' && session.username ? session.username : 'Админ' %>!
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link btn btn-outline-light btn-sm py-1 px-3" href="/logout">Выйти</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>
    <div class="container mt-4">
        <h1>Клиент: <%= client.name %></h1>

        <div class="client-card">
            <div class="row">
                <div class="col-md-6">
                    <label class="form-label mt-2">Имя</label>
                    <input type="text" class="form-control" id="clientName" value="<%= client.name %>" data-field="name" data-id="<%= client.id %>">

                    <label class="form-label mt-2">Телефон</label>
                    <input type="text" class="form-control" id="clientPhone" value="<%= client.phone || '' %>" data-field="phone" data-id="<%= client.id %>">

                    <label class="form-label mt-2">Адрес</label>
                    <input type="text" class="form-control" id="clientAddress" value="<%= client.address || '' %>" data-field="address" data-id="<%= client.id %>">
                </div>
            </div>
        </div>

        <hr class="my-4">

        <h2>Заказы клиента</h2>

        <% if (client.orders && client.orders.length > 0) { %>
            <div class="row g-3"> <!-- Bootstrap grid -->
                <% client.orders.forEach(order => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card order-card">
                            <div class="d-flex justify-content-between align-items-start">
                                <div class="flex-grow-1">
                                    <h5 class="card-title mb-0">
                                        <a href="/orders/<%= order.id %>" class="text-decoration-none" style="color: #85c1ff;">Заказ #<%= order.id %></a>
                                    </h5>
                                    <div class="order-details mt-2">
                                        <div data-label="Дата">
                                            <span class="order-info"><%= order.order_date ? new Date(order.order_date).toLocaleDateString('ru-RU') : 'N/A' %></span>
                                        </div>
                                        <div data-label="Город">
                                            <span class="order-info"><%= order.destination_city || 'N/A' %></span>
                                        </div>
                                        <div data-label="Сумма">
                                            <span class="order-stats"><%= (parseFloat(order.total_with_shipping) || 0).toFixed(2) %></span>
                                        </div>
                                        <div>
                                            <% const statusConfig = STATUS_CONFIG.ORDER_STATUSES.find(s => s.value === order.status); %>
                                            <span class="badge bg-<%= statusConfig ? statusConfig.color : 'secondary' %>">
                                                <%= order.status %>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                                <div class="order-actions">
                                    <a href="/orders/<%= order.id %>" class="btn btn-outline-light btn-sm">Детали</a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p class="text-center">У этого клиента пока нет заказов.</p>
        <% } %>

        <a href="/clients" class="btn btn-secondary mt-3">Назад к списку клиентов</a>

        <!-- Flash сообщения -->
        <% if (messages.error.length > 0) { %>
            <% messages.error.forEach(msg => { %>
                <div class="alert alert-danger"><%= msg %></div>
            <% }); %>
        <% } %>
        <% if (messages.success.length > 0) { %>
            <% messages.success.forEach(msg => { %>
                <div class="alert alert-success"><%= msg %></div>
            <% }); %>
        <% } %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const inputs = document.querySelectorAll('input[data-field][data-id]');
        inputs.forEach(input => {
          input.addEventListener('blur', async () => {
            const field = input.dataset.field;
            const id = input.dataset.id;
            const value = input.value.trim();

            try {
              const res = await fetch(`/clients/${id}`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ [field]: value })
              });
              if (!res.ok) throw new Error("Ошибка HTTP");
              const response = await res.json();
              if(response.success) {
                // Обновление flash-сообщения (опционально)
              } else {
                 alert("Ошибка обновления");
              }
            } catch (e) {
              alert("Ошибка обновления клиента");
              console.error(e);
            }
          });

          input.addEventListener("keydown", ev => {
            if (ev.key === "Enter") input.blur();
          });
        });
      });
    </script>
</body>
</html>