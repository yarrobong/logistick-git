<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Список Клиентов</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Темная тема как в Telegram */
        body { 
            background-color: #18222D; 
            color: #ffffff; 
        }

        /* Стили для карточек клиентов */
        .client-card {
            background-color: #213040; 
            border-radius: .5rem;
            transition: .2s;
            color: white;
            padding: 1rem; /* Внутренний отступ */
        }
        .client-card:hover { 
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5); 
            border-color: #3a3a3a; 
        }

        /* Кнопки */
        .btn-outline-light {
            border-color: #3a3a3a; 
            color: #d0d0d0; 
            background-color: transparent;
        }
        .btn-outline-light:hover {
            background-color: #3a3a3a; 
            color: #ffffff; 
        }

        .btn-success {
            background-color: #0088cc; 
            border-color: #0088cc;
        }
        .btn-success:hover {
            background-color: #0077b3; 
            border-color: #0077b3;
        }

        .btn-outline-danger {
            color: #ff3b30; 
            border-color: #ff3b30;
        }
        .btn-outline-danger:hover {
            background-color: #ff3b30; 
            color: #ffffff; 
        }

        /* Вводы */
        .form-control {
            background-color: #213040; 
            border: none; 
            color: #ffffff; 
        }
        .form-control:focus {
            background-color: #213040;
            color: #ffffff;
            box-shadow: 0 0 0 0.2rem rgba(133, 193, 255, 0.25); 
        }

        /* Заголовки */
        h1, h2, h3, h4, h5 {
            color: #ffffff; 
        }

        /* --- НОВЫЕ СТИЛИ ДЛЯ ПОЗИЦИЙ --- */
        .client-details {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            align-items: center;
            margin-top: 0.5rem; /* Отступ сверху */
        }

        .client-details div {
            display: flex;
            align-items: center;
            white-space: nowrap;
        }

        /* На мобильных устройствах */
        @media (max-width: 768px) {
            .client-details {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.25rem;
            }

            .client-details div {
                width: 100%; /* Занимает всю ширину */
                justify-content: flex-start; /* Выравнивание по левому краю */
                gap: 0.25rem; /* Отступ между текстом и значением */
            }

            .client-details div::before {
                content: attr(data-label) ": "; /* Добавляем подпись (например, "Телефон: ") */
                white-space: nowrap;
            }

            .client-actions {
                opacity: 1; /* Всегда видно */
                margin-top: 0.5rem; 
                align-self: center; 
            }
        }

        /* Для ширины экрана меньше 1000px */
        @media (max-width: 1000px) {
            .client-card .d-flex.justify-content-between {
                flex-direction: column; /* карточка вертикально */
                gap: 0.5rem;
            }

            .client-card .flex-grow-1 div {
                display: flex;
                flex-direction: column; /* каждый элемент на своей строке */
                gap: 0.25rem;
                width: 100%;
            }

            .client-card .flex-grow-1 div .client-info,
            .client-card .flex-grow-1 div .client-stats {
                width: 100%; /* растягиваем на всю ширину */
            }

            .client-actions {
                margin-top: 0.5rem;
                width: 100%;
                justify-content: flex-start;
            }
        }
    </style>
</head>
<body>
     <%- include('partials/navbar') %>
    <div class="container mt-4">
        <h1>Список Клиентов</h1>

        <% if (typeof clients !== 'undefined' && clients.length > 0) { %>
            <div class="row g-3"> <!-- Bootstrap grid -->
                <% clients.forEach(client => { %>
                    <div class="col-md-6 col-lg-4">
                        <div class="card client-card">
                            <div class="d-flex justify-content-between align-items-start">
                                <div class="flex-grow-1">
                                    <h5 class="card-title mb-0">
                                        <a href="/clients/<%= client.id %>" class="text-decoration-none" style="color: #85c1ff;"> <%= client.name %></a>
                                    </h5>
                                    <div class="client-details mt-2">
                                        <div data-label="Телефон">
                                            <span class="client-info"><%= client.phone || 'N/A' %></span>
                                        </div>
                                        <div data-label="Адрес">
                                            <span class="client-info"><%= client.address || 'N/A' %></span>
                                        </div>
                                        <div data-label="Заказов">
                                            <span class="client-stats"><%= client.orderCount || 0 %></span>
                                        </div>
                                        <div data-label="Последний">
                                            <span class="client-stats"><%= client.lastOrderDate ? new Date(client.lastOrderDate).toLocaleDateString('ru-RU') : 'N/A' %></span>
                                        </div>
                                    </div>
                                </div>
                                <div class="client-actions">
                                    <a href="/clients/<%= client.id %>" class="btn btn-outline-light btn-sm">Просмотр</a>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <p class="text-center">Клиентов пока нет.</p>
        <% } %>

        <!-- Flash сообщения -->
        <% if (messages.error.length > 0) { %>
            <% messages.error.forEach(msg => { %>
                <div class="alert alert-danger"><%= msg %></div>
            <% }); %>
        <% } %>
        <% if (messages.success.length > 0) { %>
            <% messages.success.forEach(msg => { %>
                <div class="alert alert-success"><%= msg %></div>
            <% }); %>
        <% } %>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>